import { Canvas, Meta, Story, ArgsTable, Source } from '@storybook/addon-docs';
import { renderComponent } from './utils/renderComponent';
import RwProgressBar from "../src/components/RwProgressBar";
import {Types} from "./utils/componentTypes";
import {getSourceCode} from "./utils/getSourceCode";

<Meta
  title="RwProgressBar"
  component={RwProgressBar}
  argTypes={{
    orientation: {
      options: [
        'horizontal',
        'vertical',
      ],
      table: {
        type: { summary: Types.String },
        defaultValue: { summary: undefined },
      },
    },
  }}
/>

export const Template = renderComponent({ Component: RwProgressBar, slots: [] });

# RwProgressBar

With `MDX`, we can define a story for `RwProgressBar` right in the middle of our
Markdown documentation.

<Canvas withSource='none'>
  <Story
    name="Default"
    args={{
      orientation: 'horizontal'
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

export const DEFAULT_CODE = await getSourceCode('RwProgressBar', 'DefaultCode')

<Source
  language='html'
  dark
  format={false}
  code={DEFAULT_CODE}
/>

<ArgsTable story="Default" />
