import { Canvas, Meta, Story, ArgsTable, Source } from '@storybook/addon-docs';
import RwButton from '../src/components/RwButton';
import { renderComponent } from './utils/renderComponent';
import {severalTypes, Types} from "./utils/componentTypes";

<Meta
  title="RwButton"
  component={RwButton}
  argTypes= {{
    default: {
      control: 'text',
      description: 'Slot to render button text',
      table: {
        type: { summary: Types.String },
        defaultValue: { summary: undefined },
      },
    },
    loading: {
      control: 'boolean',
      description: 'Button loading state',
      table: {
        type: { summary: Types.Boolean },
        defaultValue: { summary: undefined },
      },
    },
    loadingSlot: {
      control: 'text',
      description: 'Slot to render button text',
      table: {
        type: { summary: Types.Slot },
        defaultValue: { summary: undefined },
      },
    },
    click: {
      control: false,
      description: 'Click event',
      table: {
        type: { summary: '() => void' },
        defaultValue: { summary: undefined },
      },
    },
    type: {
      control: false,
      description: 'Slot to render button text',
      table: {
        type: { summary: severalTypes(['submit', 'button']) },
        defaultValue: { summary: 'button' },
      },
    },
  }}
/>

export const Template = renderComponent({ Component: RwButton, slots: ['default', 'loadingSlot'] });

# RwButton

With `MDX`, we can define a story for `RwButton` right in the middle of our
Markdown documentation.

<Canvas withSource='none'>
  <Story
    name="Default"
    args={{
      default: 'Button',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Source
  language='html'
  dark
  format={false}
  code={'<RwButton>Button</RwButton>'}
/>

<ArgsTable story="Default" />
