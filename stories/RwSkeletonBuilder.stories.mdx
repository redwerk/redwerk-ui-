import { Canvas, Meta, Story, ArgsTable, Source } from '@storybook/addon-docs';
import RwSkeletonBuilder from '../src/components/RwSkeletonBuilder/RwSkeletonBuilder.vue';
import { renderComponent } from './utils/renderComponent';
import { Types } from "./utils/componentTypes";
import {getSourceCode} from "./utils/getSourceCode";

<Meta
  title="RwSkeletonBuilder"
  component={RwSkeletonBuilder}
  argTypes= {{
    structure: {
      table: {
        type: { summary: Types.ArrayOfStrings },
        defaultValue: {summary: []},
      },
    },
    loading: {
      table: {
        type: { summary: Types.Boolean },
        defaultValue: {summary: false},
      },
    },
    horizontal: {
      table: {
        type: { summary: Types.Boolean },
        defaultValue: {summary: false},
      },
    },
    behavior: {
      table: {
        type: { summary: Types.String },
        defaultValue: {summary: 'gradient'},
      },
    },
  }}
/>

export const Template = renderComponent({ Component: RwSkeletonBuilder, slots: [] });

# RwSkeletonBuilder

With `MDX`, we can define a story for `RwSkeletonBuilder` right in the middle of our
Markdown documentation.

<Canvas withSource='none'>
  <Story
    name="Default"
    args={{
      structure: [
        'circle',
        'square',
        'rectangle',
        'bar',
      ],
      loading: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

export const DEFAULT_CODE = await getSourceCode('RwSkeletonBuilder', 'DefaultCode')

<Source
  language='html'
  dark
  format={false}
  code={DEFAULT_CODE}
/>

<ArgsTable story="Default" />
